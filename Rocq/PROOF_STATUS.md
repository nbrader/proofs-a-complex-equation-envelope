# Proof Status

- `ComplexEnvelope.v`: complete; main theorem `envelope_characterizes_solutions` proven (uses classical logic and functional extensionality).
- No remaining admits in the standard version.
